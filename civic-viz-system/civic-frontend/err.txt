
> civic-frontend@0.0.0 build
> vite build

[36mvite v5.4.21 [32mbuilding for production...[36m[39m
transforming...
[32m✓[39m 13 modules transformed.
[31mx[39m Build failed in 1.05s
[31merror during build:
[31m[vite:esbuild] Transform failed with 1 error:
C:/Users/RAUNAK KUMAR GIRI/AppData/Local/Packages/5319275A.WhatsAppDesktop_cv1g1gvanyjgm/LocalState/sessions/A1A3ED5E04D5B943BCCA694ED4FB9BE8E8222D95/transfers/2026-13/indiainnovates/civic-viz-system/civic-frontend/src/pages/Visualization.jsx:1:66: ERROR: Syntax error "n"[31m
file: [36mC:/Users/RAUNAK KUMAR GIRI/AppData/Local/Packages/5319275A.WhatsAppDesktop_cv1g1gvanyjgm/LocalState/sessions/A1A3ED5E04D5B943BCCA694ED4FB9BE8E8222D95/transfers/2026-13/indiainnovates/civic-viz-system/civic-frontend/src/pages/Visualization.jsx:1:66[31m
[33m
[33mSyntax error "n"[33m
1  |  import { useState, useRef, useEffect, useCallback } from 'react';\nimport * as d3 from 'd3';\nimport axios from 'axios';\nimport { motion, AnimatePresence } from 'framer-motion';\nimport { Canvas } from '@react-three/fiber';\nimport { OrbitControls, Stars, PerspectiveCamera, Html, Line } from '@react-three/drei';\nimport { Search, ChevronLeft, Maximize2, Box, Activity, Info, Zap, Layers, Globe, BarChart3, Network, CheckCircle, XCircle, Clock } from 'lucide-react';\nimport { useNavigate } from 'react-router-dom';\n\n// ─── Status color map ─────────────────────────────────────────────────────────\nconst STATUS_COLORS = {\n  Pending:        '#00f2ff',  // blue\n  Assigned:       '#00f2ff',  // blue\n  'In Progress':  '#bc13fe',  // purple\n  Completed:      '#ffb300',  // amber (pulsing - awaiting review)\n  AwaitingReview: '#ffb300',  // amber\n  Resolved:       '#1a4a1a',  // dim dark green (faded)\n  Reopened:       '#ff003c',  // red\n};\n\nfunction getStatusColor(c) {\n  if (c.final_status === 'Resolved') return STATUS_COLORS.Resolved;\n  if (c.final_status === 'AwaitingReview' || c.status === 'Completed') return STATUS_COLORS.Completed;\n  if (c.final_status === 'Reopened' || c.status === 'Reopened') return STATUS_COLORS.Reopened;\n  return STATUS_COLORS[c.status] || '#00f2ff';\n}\n\n// ─── Status Badge ─────────────────────────────────────────────────────────────\nconst BADGE_STYLES = {\n  Resolved:       'text-green-400 border-green-400/30 bg-green-400/10',\n  Pending:        'text-cyber-red border-cyber-red/30 bg-cyber-red/10',\n  Assigned:       'text-cyber-blue border-cyber-blue/30 bg-cyber-blue/10',\n  'In Progress':  'text-purple-400 border-purple-400/30 bg-purple-400/10',\n  Completed:      'text-amber-400 border-amber-400/30 bg-amber-400/10',\n  AwaitingReview: 'text-amber-400 border-amber-400/30 bg-amber-400/10',\n  Reopened:       'text-red-400 border-red-400/30 bg-red-400/10',\n};\n\nfunction StatusBadge({ status, finalStatus }) {\n  const key = finalStatus === 'AwaitingReview' ? 'AwaitingReview'\n    : finalStatus === 'Resolved' ? 'Resolved'\n    : finalStatus === 'Reopened' ? 'Reopened'\n    : status;\n  const display = key === 'AwaitingReview' ? 'Awaiting Review' : key;\n  const cls = BADGE_STYLES[key] || BADGE_STYLES.Pending;\n  const pulse = key === 'Awaiting Review' || key === 'AwaitingReview' || key === 'Completed';\n  return (\n    <span className={`inline-flex items-center gap-1.5 text-[10px] font-black uppercase tracking-widest px-3 py-1 rounded-full border ${cls}`}>\n      <span className={`w-1.5 h-1.5 rounded-full bg-current ${pulse ? 'animate-pulse' : ''}`} />\n      {display}\n    </span>\n  );\n}\n\n// ─── AdminReviewModule ────────────────────────────────────────────────────────\nconst AdminReviewModule = ({ complaint, onReviewed }) => {\n  const [note, setNote] = useState('');\n  const [loading, setLoading] = useState(null); // 'approve' | 'reject'\n\n  const handleAction = async (action) => {\n    setLoading(action);\n    try {\n      await axios.patch(`http://localhost:5000/api/admin/complaints/${complaint.id}/review`, {\n        action,\n        admin_review_note: note,\n      });\n      onReviewed(complaint.id, action === 'approve' ? 'Resolved' : 'Reopened');\n    } catch (err) {\n      console.error('Review failed:', err);\n      alert(err.response?.data?.error || 'Review action failed');\n    }\n    setLoading(null);\n  };\n\n  return (\n    <div className="mt-4 pt-4 border-t border-amber-400/20 space-y-4">\n      {/* Header */}\n      <div className="flex items-center gap-2">\n        <div className="w-1.5 h-1.5 rounded-full bg-amber-400 animate-pulse" />\n        <span className="text-[10px] font-black uppercase tracking-[0.25em] text-amber-400">Admin Review Required</span>\n      </div>\n\n      {/* Dept response */}\n      {complaint.department_response && (\n        <div className="bg-white/3 rounded-xl border border-white/5 p-4">\n          <div className="text-[9px] font-black uppercase tracking-widest text-slate-500 mb-2">Department Field Report</div>\n          <p className="text-xs text-white/80 italic leading-relaxed">"{complaint.department_response}"</p>\n        </div>\n      )}\n\n      {/* Resolution image */}\n      {complaint.resolution_image && (\n        <div>\n          <div className="text-[9px] font-black uppercase tracking-widest text-slate-500 mb-2">Resolution Proof</div>\n          <img\n            src={`http://localhost:5000${complaint.resolution_image}`}\n            alt="Resolution"\n            className="w-full h-40 object-cover rounded-xl border border-white/10"\n          />\n        </div>\n      )}\n\n      {/* Admin note input */}\n      <textarea\n        value={note}\n        onChange={e => setNote(e.target.value)}\n        placeholder="Optional admin review note..."\n        className="w-full bg-black/50 border border-white/10 rounded-lg text-xs p-3 text-white h-16 resize-none outline-none focus:border-amber-400 transition"\n      />\n\n      {/* Action buttons */}\n      <div className="flex gap-3">\n        <button\n          onClick={() => handleAction('approve')}\n          disabled={!!loading}\n          className="flex-1 flex items-center justify-center gap-2 py-3 rounded-xl font-black uppercase tracking-widest text-[10px] border transition\n            bg-green-400/10 text-green-400 border-green-400/30 hover:bg-green-400 hover:text-black disabled:opacity-50"\n        >\n          {loading === 'approve'\n            ? <span className="animate-spin w-3 h-3 border-2 border-current border-t-transparent rounded-full" />\n            : <CheckCircle size={12} />\n          }\n          Approve & Resolve\n        </button>\n        <button\n          onClick={() => handleAction('reject')}\n          disabled={!!loading}\n          className="flex-1 flex items-center justify-center gap-2 py-3 rounded-xl font-black uppercase tracking-widest text-[10px] border transition\n            bg-red-400/10 text-red-400 border-red-400/30 hover:bg-red-400 hover:text-black disabled:opacity-50"\n        >\n          {loading === 'reject'\n            ? <span className="animate-spin w-3 h-3 border-2 border-current border-t-transparent rounded-full" />\n            : <XCircle size={12} />\n          }\n          Reject & Reopen\n        </button>\n      </div>\n    </div>\n  );\n};\n\n// ─── 3D Data Node ─────────────────────────────────────────────────────────────\nconst DataNode3D = ({ position, color, label, details, onClick }) => {\n  const [hovered, setHovered] = useState(false);\n  const nodeColor = getStatusColor(details);\n  const isResolved = details.final_status === 'Resolved';\n  const isAwaiting = details.final_status === 'AwaitingReview' || details.status === 'Completed';\n\n  return (\n    <mesh\n      position={position}\n      scale={isResolved ? 0.6 : hovered ? 1.3 : 1}\n      onClick={(e) => { e.stopPropagation(); onClick(details); }}\n      onPointerOver={(e) => { e.stopPropagation(); setHovered(true); document.body.style.cursor = 'pointer'; }}\n      onPointerOut={(e) => { e.stopPropagation(); setHovered(false); document.body.style.cursor = 'default'; }}\n    >\n      <sphereGeometry args={[0.3, 32, 32]} />\n      <meshStandardMaterial\n        color={hovered ? '#ffffff' : nodeColor}\n        emissive={nodeColor}\n        emissiveIntensity={isResolved ? 0.5 : hovered ? 4 : isAwaiting ? 3 : 2}\n        transparent={isResolved}\n        opacity={isResolved ? 0.4 : 1}\n      />\n      {hovered && (\n        <Html distanceFactor={15} center zIndexRange={[100, 200]}>\n          <div className="glass p-5 rounded-2xl border border-white/20 w-72 text-left backdrop-blur-xl bg-black/80 shadow-[0_0_30px_rgba(0,242,255,0.4)] pointer-events-none">\n            {details.imageUrl && (\n              <img src={details.imageUrl} alt="Issue" className="w-full h-32 object-cover rounded-xl mb-3 border border-white/10" />\n            )}\n            <div className="text-[10px] font-black uppercase tracking-widest mb-2 pb-2 border-b border-white/10 flex justify-between" style={{ color: nodeColor }}>\n              <span>Voter: {label}</span>\n              <Network size={12} />\n            </div>\n            <div className="text-sm text-white font-medium mb-2 leading-relaxed line-clamp-3">{details.description}</div>\n            {details.address && (\n              <div className="text-[10px] text-slate-400 mb-2 flex items-center gap-1">📍 {details.address}</div>\n            )}\n            <div className="flex justify-between items-center mt-2 px-3 py-2 bg-white/5 rounded-lg border border-white/5">\n              <span className="text-[10px] uppercase tracking-widest text-slate-400">{new Date(details.createdAt).toLocaleDateString()}</span>\n              <span className="text-[10px] font-black uppercase tracking-widest" style={{ color: nodeColor }}>\n                {details.final_status === 'AwaitingReview' ? 'Awaiting Review' : details.final_status || details.status}\n              </span>\n            </div>\n          </div>\n        </Html>\n      )}\n    </mesh>\n  );\n};\n\n// ─── Complaint Admin Card ─────────────────────────────────────────────────────\nconst ComplaintAdminCard = ({ c, isSelected, toggleSelection, onUpdate }) => {\n  const [status, setStatus] = useState(c.status);\n  const [notes, setNotes] = useState(c.adminComments || '');\n  const [loading, setLoading] = useState(false);\n\n  // Determine if this card needs admin review\n  const needsReview = (c.status === 'Completed' || c.final_status === 'AwaitingReview')\n    && c.final_status !== 'Resolved' && c.final_status !== 'Reopened';\n\n  const isResolved = c.final_status === 'Resolved';\n  const isReopened = c.status === 'Reopened' || c.final_status === 'Reopened';\n\n  const handleUpdate = async () => {\n    setLoading(true);\n    try {\n      await axios.patch(`http://localhost:5000/api/admin/complaints/${c._id || c.id}`, {\n        status,\n        adminComments: notes,\n      });\n      const simResponse = status === 'In Progress'\n        ? 'Under process by field agents.'\n        : status === 'Resolved' ? 'Work completed and verified.' : '';\n      onUpdate(c._id || c.id, { status, adminComments: notes, department_response: simResponse });\n    } catch (err) { console.error(err); }\n    setLoading(false);\n  };\n\n  const handleReviewed = (id, finalStatus) => {\n    onUpdate(id, {\n      status: finalStatus,\n      final_status: finalStatus,\n    });\n  };\n\n  const steps = ['Pending', 'Assigned', 'In Progress', 'Completed', 'Resolved'];\n  const currentStep = steps.indexOf(c.final_status === 'Resolved' ? 'Resolved' : c.status);\n\n  const cardBorderClass = needsReview\n    ? 'border-amber-400/40 shadow-[0_0_15px_rgba(255,179,0,0.1)]'\n    : isReopened ? 'border-red-400/30'\n    : isResolved ? 'border-green-400/10'\n    : isSelected ? 'border-cyber-blue shadow-[0_0_15px_rgba(0,242,255,0.2)]'\n    : 'border-white/5';\n\n  return (\n    <div className={`glass p-5 rounded-2xl border ${cardBorderClass} hover:bg-white/5 transition-all group relative overflow-hidden`}>\n      {/* Top accent for awaiting review */}\n      {needsReview && <div className="absolute top-0 left-0 w-full h-0.5 bg-amber-400 animate-pulse" />}\n      {isReopened && <div className="absolute top-0 left-0 w-full h-0.5 bg-red-400" />}\n\n      {/* Header */}\n      <div className="flex justify-between items-start mb-3">\n        <div className="flex items-center gap-3">\n          <input\n            type="checkbox"\n            checked={isSelected}\n            onChange={() => toggleSelection(c._id || c.id)}\n            className="w-4 h-4 rounded border-white/20 bg-black/50 text-cyber-blue cursor-pointer"\n          />\n          <span className="text-xs font-black uppercase tracking-[0.2em] text-slate-400">{c.voter_id}</span>\n        </div>\n        <StatusBadge status={c.status} finalStatus={c.final_status} />\n      </div>\n\n      {/* Evidence image */}\n      {c.imageUrl && (\n        <img\n          src={c.imageUrl.startsWith('http') ? c.imageUrl : `http://localhost:5000${c.imageUrl}`}\n          alt="Evidence"\n          className="w-full h-32 object-cover rounded-xl mb-4 border border-white/10 group-hover:scale-[1.02] transition-transform duration-300"\n        />\n      )}\n\n      <p className="text-sm text-white font-medium leading-relaxed mb-3">"{c.description}"</p>\n\n      {c.address && (\n        <div className="flex items-center gap-2 text-[11px] text-slate-400 font-medium bg-white/5 px-3 py-2 rounded-lg border border-white/5 mb-3">\n          <span>📍</span><span>{c.address}</span>\n        </div>\n      )}\n\n      {/* Dept / Assignment Info */}\n      <div className="bg-black/40 rounded-xl p-4 border border-white/5 mb-3 space-y-3">\n        <div className="grid grid-cols-2 gap-2 text-[10px] uppercase font-bold tracking-wider">\n          <div>\n            <span className="text-slate-500 block mb-1">Dept Mapping</span>\n            <span className="text-cyber-blue">{c.department_name || 'Unmapped'}</span>\n          </div>\n          <div>\n            <span className="text-slate-500 block mb-1">Assigned To</span>\n            <span className={c.assigned_department ? 'text-cyber-purple' : 'text-slate-600'}>\n              {c.assigned_department || 'Unassigned'}\n            </span>\n          </div>\n        </div>\n\n        {/* Status Timeline */}\n        <div className="pt-2 border-t border-white/5">\n          <div className="flex justify-between items-center text-[9px] font-bold text-slate-500 uppercase tracking-widest mb-2">\n            {steps.map((step, idx) => (\n              <span key={step} className={\n                idx <= currentStep\n                  ? step === 'Resolved' ? 'text-green-400'\n                  : step === 'Completed' ? 'text-amber-400'\n                  : 'text-cyber-blue'\n                  : ''\n              }>{step}</span>\n            ))}\n          </div>\n          <div className="w-full h-1 bg-white/10 rounded-full overflow-hidden">\n            <div\n              className={`h-full transition-all duration-500 bg-gradient-to-r ${\n                c.final_status === 'Resolved' ? 'from-green-600 to-green-400'\n                : (c.status === 'Completed' || c.final_status === 'AwaitingReview') ? 'from-amber-600 to-amber-400'\n                : isReopened ? 'from-red-600 to-red-400'\n                : 'from-cyber-blue/50 to-cyber-blue'\n              }`}\n              style={{ width: `${(Math.max(0, currentStep) / (steps.length - 1)) * 100}%` }}\n            />\n          </div>\n        </div>\n\n        {/* Dept response preview */}\n        {c.department_response && !needsReview && (\n          <div className="text-[10px] text-yellow-400/80 italic mt-2 p-2 bg-yellow-400/5 rounded border border-yellow-400/10">\n            Dept: {c.department_response}\n          </div>\n        )}\n\n        {/* Admin review note */}\n        {c.admin_review_note && (\n          <div className="text-[10px] text-purple-400/80 italic mt-1 p-2 bg-purple-400/5 rounded border border-purple-400/10">\n            Review Note: {c.admin_review_note}\n          </div>\n        )}\n      </div>\n\n      {/* ── ADMIN REVIEW MODULE (if status = Completed and awaiting) ── */}\n      {needsReview && (\n        <AdminReviewModule complaint={c} onReviewed={handleReviewed} />\n      )}\n\n      {/* ── STANDARD ADMIN CONTROLS (if not resolved / not awaiting) ── */}\n      {!needsReview && !isResolved && (\n        <div className="flex flex-col gap-2 mt-4 pt-4 border-t border-white/5">\n          <div className="flex gap-2">\n            <select\n              value={status}\n              onChange={e => setStatus(e.target.value)}\n              className="bg-black/50 border border-white/10 rounded-lg text-xs p-2 flex-1 text-white focus:outline-none focus:border-cyber-blue"\n            >\n              {['Pending', 'Assigned', 'In Progress', 'Completed', 'Resolved'].map(s => (\n                <option key={s} value={s}>{s}</option>\n              ))}\n            </select>\n            <button\n              onClick={handleUpdate}\n              disabled={loading || (status === c.status && notes === (c.adminComments || ''))}\n              className="px-4 py-2 bg-white/10 hover:bg-cyber-blue/20 text-cyber-blue rounded-lg text-[10px] font-black uppercase tracking-widest transition-colors disabled:opacity-50 border border-cyber-blue/30"\n            >\n              {loading ? '...' : 'Save'}\n            </button>\n          </div>\n          <textarea\n            value={notes}\n            onChange={e => setNotes(e.target.value)}\n            placeholder="Admin notes / directives for department..."\n            className="w-full bg-black/50 border border-white/10 rounded-lg text-xs p-2 text-white h-16 resize-none focus:outline-none focus:border-cyber-blue"\n          />\n        </div>\n      )}\n\n      {/* Resolved state */}\n      {isResolved && (\n        <div className="flex items-center gap-2 text-[10px] text-green-400 bg-green-400/5 border border-green-400/20 p-3 rounded-lg mt-4">\n          <CheckCircle size={12} />\n          <span>Fully Resolved — Closed</span>\n        </div>\n      )}\n    </div>\n  );\n};\n\n
   |                                                                    ^
2  |  // ─── 3D Orbital Engine ────────────────────────────────────────────────────────
3  |  const OrbitalGalaxy = ({ 
[31m
    at failureErrorWithLog (C:\Users\RAUNAK KUMAR GIRI\AppData\Local\Packages\5319275A.WhatsAppDesktop_cv1g1gvanyjgm\LocalState\sessions\A1A3ED5E04D5B943BCCA694ED4FB9BE8E8222D95\transfers\2026-13\indiainnovates\civic-viz-system\civic-frontend\node_modules\esbuild\lib\main.js:1472:15)
    at C:\Users\RAUNAK KUMAR GIRI\AppData\Local\Packages\5319275A.WhatsAppDesktop_cv1g1gvanyjgm\LocalState\sessions\A1A3ED5E04D5B943BCCA694ED4FB9BE8E8222D95\transfers\2026-13\indiainnovates\civic-viz-system\civic-frontend\node_modules\esbuild\lib\main.js:755:50
    at responseCallbacks.<computed> (C:\Users\RAUNAK KUMAR GIRI\AppData\Local\Packages\5319275A.WhatsAppDesktop_cv1g1gvanyjgm\LocalState\sessions\A1A3ED5E04D5B943BCCA694ED4FB9BE8E8222D95\transfers\2026-13\indiainnovates\civic-viz-system\civic-frontend\node_modules\esbuild\lib\main.js:622:9)
    at handleIncomingPacket (C:\Users\RAUNAK KUMAR GIRI\AppData\Local\Packages\5319275A.WhatsAppDesktop_cv1g1gvanyjgm\LocalState\sessions\A1A3ED5E04D5B943BCCA694ED4FB9BE8E8222D95\transfers\2026-13\indiainnovates\civic-viz-system\civic-frontend\node_modules\esbuild\lib\main.js:677:12)
    at Socket.readFromStdout (C:\Users\RAUNAK KUMAR GIRI\AppData\Local\Packages\5319275A.WhatsAppDesktop_cv1g1gvanyjgm\LocalState\sessions\A1A3ED5E04D5B943BCCA694ED4FB9BE8E8222D95\transfers\2026-13\indiainnovates\civic-viz-system\civic-frontend\node_modules\esbuild\lib\main.js:600:7)
    at Socket.emit (node:events:518:28)
    at addChunk (node:internal/streams/readable:561:12)
    at readableAddChunkPushByteMode (node:internal/streams/readable:512:3)
    at Readable.push (node:internal/streams/readable:392:5)
    at Pipe.onStreamRead (node:internal/stream_base_commons:189:23)[39m
